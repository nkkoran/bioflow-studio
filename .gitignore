# Dependencies — npm install re-creates this; it's huge (~500MB)
node_modules/

# Built output — generated files, not source
dist/
dist-electron/
out/
release/

# Electron-store runtime data, if generated in-tree. Keep source modules tracked.
electron/store/*.json

# macOS junk
.DS_Store

# Environment secrets
.env
*.pem
*.key

# Claude personal overrides
CLAUDE.local.md
