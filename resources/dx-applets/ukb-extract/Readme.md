BioFlow Studio bundles this DNAnexus applet for fixed UK Biobank Spark extraction jobs.

Inputs are JSON data files plus simple string parameters. User-provided field ids and rename
labels are always passed as data, never interpolated into code.
